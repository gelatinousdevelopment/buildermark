# Frontend

## Architecture

- Svelte 5 project
- Static adapter (no server svelte files)

## Instructions

- Always check `svelte-check` and `eslint` after writing code
- Always run `prettier` to format after writing code
